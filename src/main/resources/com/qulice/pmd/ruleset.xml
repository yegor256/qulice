<?xml version="1.0" encoding="UTF-8"?>
<!--
* SPDX-FileCopyrightText: Copyright (c) 2011-2026 Yegor Bugayenko
* SPDX-License-Identifier: MIT
-->
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://pmd.sourceforge.net/ruleset/2.0.0" name="Qulice Ruleset" xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.github.io/schema/ruleset_2_0_0.xsd">
  <description>
    This ruleset checks code for potential mess
  </description>
  <rule ref="category/java/bestpractices.xml">
    <!--
    GuardLogStatement wants every log call wrapped in an 'isDebugEnabled'
    test. Qulice logs through jcabi-log, whose Logger already checks the
    level before it formats anything, so the guard would add a branch that
    buys nothing. It fires 18 times here.
    -->
    <exclude name="GuardLogStatement"/>
    <!--
    AvoidUsingHardCodedIP is excluded from the bulk import here and
    re-enabled below with a violationSuppressXPath that ignores
    hard-coded IP literals inside unit-test classes (those whose
    simple name ends with 'Test', 'IT', 'TestCase' or 'ITCase'),
    where IP literals are commonly used as fixture data.
    Downstream: https://github.com/yegor256/qulice/issues/713
    -->
    <exclude name="AvoidUsingHardCodedIP"/>
    <!--
    UnnecessaryVarargsArrayCreation is excluded due to a false positive
    when the explicit array is needed to disambiguate overloaded
    methods/constructors: removing it produces a compile error.
    Re-enable once PMD fixes it upstream.
    Upstream: https://github.com/pmd/pmd/issues/6611
    Downstream: https://github.com/yegor256/qulice/issues/1525
    -->
    <exclude name="UnnecessaryVarargsArrayCreation"/>
    <!--
    UnitTestShouldIncludeAssert is excluded because it doesn't traverse
    lambda bodies and therefore misses assertions placed inside lambdas.
    Re-enable once PMD fixes it upstream; until then, we use our own
    UnitTestShouldIncludeAssertRule that crosses lambda boundaries.
    Upstream: https://github.com/pmd/pmd/issues/4272
    Downstream: https://github.com/yegor256/qulice/issues/1524
    -->
    <exclude name="UnitTestShouldIncludeAssert"/>
    <!--
    UnitTestContainsTooManyAsserts is excluded because it counts any
    method invocation whose simple name starts with 'assert', 'check'
    or 'verify', producing false positives for unrelated APIs such as
    pull.checks() (jcabi-github) or Mockito.verify(...). It also
    counts Assertions.assertThrows, which breaks the common idiom of
    wrapping assertThrows(...).getMessage() inside assertThat. We use
    our own UnitTestContainsTooManyAssertsRule that only counts the
    known JUnit/Hamcrest assertion methods and ignores assertThrows.
    Downstream: https://github.com/yegor256/qulice/issues/1519
    Downstream: https://github.com/yegor256/qulice/issues/1606
    -->
    <exclude name="UnitTestContainsTooManyAsserts"/>
    <exclude name="UnusedPrivateField"/>
    <!--
    UnitTestAssertionsShouldIncludeMessage (a.k.a. the deprecated
    JUnitAssertionsShouldIncludeMessage) treats the two-argument
    Hamcrest form MatcherAssert.assertThat(String reason, boolean
    assertion) as missing a message, because it matches any
    assertThat call with two arguments. We re-enable the rule with a
    violationSuppressXPath that skips this specific overload, where
    the first argument IS the message.
    Downstream: https://github.com/yegor256/qulice/issues/1315
    -->
    <exclude name="UnitTestAssertionsShouldIncludeMessage"/>
    <!--
    UnitTestShouldUseTestAnnotation (formerly
    JUnit4TestShouldUseTestAnnotation) flags any public method whose
    name starts with 'test' in a class whose name ends with 'Test'
    when the method is not annotated with a known test annotation.
    That breaks classes that use non-JUnit conventions such as
    g4s8/oot, whose test entry point is a bare
    'public static void test()' method with no annotation. We
    re-enable the rule below with a violationSuppressXPath that
    ignores such oot-style classes.
    Downstream: https://github.com/yegor256/qulice/issues/1064
    -->
    <exclude name="UnitTestShouldUseTestAnnotation"/>
    <!--
    ImplicitFunctionalInterface counts only the abstract methods it can
    see, so an interface that declares one method of its own but extends
    other interfaces adding more abstract methods is wrongly reported as
    functional whenever those super-interfaces cannot be resolved (Qulice
    runs PMD without an auxiliary classpath, so types declared elsewhere
    are invisible). Adding the suggested @FunctionalInterface then fails to
    compile. We use our own ImplicitFunctionalInterface rule instead, which
    stays silent unless the whole super-interface hierarchy resolves and the
    interface truly has a single abstract method overall.
    Downstream: https://github.com/yegor256/qulice/issues/1704
    -->
    <exclude name="ImplicitFunctionalInterface"/>
  </rule>
  <rule ref="category/java/bestpractices.xml/AvoidUsingHardCodedIP">
    <properties>
      <property name="violationSuppressXPath">
        <value><![CDATA[
          .[
            ancestor::ClassDeclaration[1][
              ends-with(@SimpleName,'Test')
              or ends-with(@SimpleName,'IT')
              or ends-with(@SimpleName,'TestCase')
              or ends-with(@SimpleName,'ITCase')
            ]
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule ref="category/java/bestpractices.xml/UnitTestShouldUseTestAnnotation">
    <properties>
      <property name="violationSuppressXPath">
        <value><![CDATA[
          .[
            ancestor::ClassDeclaration[1]
              /ClassBody
              /MethodDeclaration[
                @Name='test' and @Static=true() and @Visibility='public'
              ]
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule ref="category/java/bestpractices.xml/UnitTestAssertionsShouldIncludeMessage">
    <properties>
      <property name="violationSuppressXPath">
        <value><![CDATA[
          .[
            @MethodName = 'assertThat'
            and count(ArgumentList/*) = 2
            and ArgumentList/*[1][pmd-java:typeIs('java.lang.String')]
            and ArgumentList/*[2][pmd-java:typeIs('boolean') or pmd-java:typeIs('java.lang.Boolean')]
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <!--
  UnusedPrivateField treats statement-level increment or decrement
  and compound assignments (like plus-equals or minus-equals) as
  write-only, so a field that is read-modified-written but never
  read in a pure-read position gets flagged as unused. The
  suppression below keeps the rule active but ignores violations
  for fields that are referenced by an increment, decrement or
  compound assignment anywhere in the enclosing class or its
  nested classes.
  Downstream: https://github.com/yegor256/qulice/issues/1520
  -->
  <rule ref="category/java/bestpractices.xml/UnusedPrivateField">
    <properties>
      <property name="violationSuppressXPath">
        <value><![CDATA[
          .[
            @Name = ancestor::ClassDeclaration[1]
              //(
                UnaryExpression[@Operator = ('++', '--')]
                | AssignmentExpression[@Operator = ('+=', '-=', '*=', '/=', '%=', '&=', '|=', '^=', '<<=', '>>=', '>>>=')]
              )
              //(FieldAccess | VariableAccess)/@Name
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule ref="category/java/codestyle.xml">
    <!--
    AtLeastOneConstructor wants every class to declare a constructor, even
    the ones whose every field is assigned inline. Adding an empty
    constructor to satisfy it would collide with
    'ConstructorShouldDoInitialization' below, which forbids inline field
    initializers as soon as a constructor exists: together the two rules
    demand that every class move all its initialization into a
    constructor, which is a design decision and not a style one. It fires
    149 times here.
    -->
    <exclude name="AtLeastOneConstructor"/>
    <!--
    ShortClassName rejects a type name shorter than five characters.
    Qulice asks for names that are single nouns, and short nouns such as
    'Xml' or 'Pmd' are the best names available for those concepts.
    -->
    <exclude name="ShortClassName"/>
    <!--
    ShortMethodName rejects a method name shorter than three characters.
    Qulice asks that a method be named as a single noun or verb, and the
    Checkstyle 'MethodName' module it already runs bounds the length with
    the format '^(id|[a-z]{2,}[a-zA-Z]+)$'.
    -->
    <exclude name="ShortMethodName"/>
    <!--
    ShortVariable rejects any name shorter than three characters, which
    contradicts the Checkstyle rules Qulice already runs: 'LocalVariableName',
    'ParameterName' and 'MemberName' all use the format '^(id|[a-z]{3,12})$',
    which allows 'id' by name. PMD reports that very identifier, so the two
    checkers would disagree on the same line. Length is already bounded by
    those formats.
    -->
    <exclude name="ShortVariable"/>
    <!--
    ClassNamingConventions, at its defaults, only asks that a type name be
    Pascal case, which is exactly what the Checkstyle 'TypeName' module
    Qulice already runs enforces. Enabling it would report every badly
    named type twice. Its interesting mode, naming utility classes
    '*Utils', stays off because Qulice rejects utility classes outright.
    -->
    <exclude name="ClassNamingConventions"/>
    <!--
    CommentDefaultAccessModifier wants a '/* default */' marker on every
    package-private member. Qulice already forbids stating what the code
    states, and the marker is exactly that: a comment that repeats the
    absence of a modifier. It fires 59 times here.
    -->
    <exclude name="CommentDefaultAccessModifier"/>
    <!--
    CallSuperInConstructor wants an explicit 'super()' in every
    constructor of every class, including the ones that extend nothing.
    Qulice allows only assignments in a primary constructor, and a
    'super()' call the compiler inserts anyway is not one of them.
    -->
    <exclude name="CallSuperInConstructor"/>
    <!--
    UnnecessaryFullyQualifiedName contradicts our own
    'AvoidDirectAccessToStaticFields' rule below. That rule demands that
    a static field be read as 'ClassName.FIELD', while this one reports
    the very same qualifier as unnecessary because the field is already in
    scope. The two cannot both be satisfied, and the qualified form is the
    one Qulice asks for, so this rule loses. It fires 299 times here.
    -->
    <exclude name="UnnecessaryFullyQualifiedName"/>
    <!--
    UseDiamondOperator is excluded from the bulk import here and re-enabled
    below with a violationSuppressXPath, because PMD's type inference does
    not always agree with javac and the rule reports diamonds that do not
    compile. Re-enable the bulk form (drop the block below) once PMD's
    inference matches javac.
    Downstream: https://github.com/yegor256/qulice/issues/1685
    -->
    <exclude name="UseDiamondOperator"/>
    <!--
    MethodNamingConventions is re-enabled below with a
    violationSuppressXPath that ignores the methods of a JNA binding.
    -->
    <exclude name="MethodNamingConventions"/>
  </rule>
  <!--
  MethodNamingConventions asks every method name to start with a lower
  case letter, while a type that extends com.sun.jna.Library names its
  methods after the native functions they map, spelled the way the
  library that exports them spells them. Renaming one breaks the
  binding, so the suppression below skips them. Qulice runs PMD without
  an auxiliary classpath, so 'Library' never resolves to a type and the
  name in the extends or implements clause is trusted only when it is
  fully qualified or when the file imports it from com.sun.jna.
  -->
  <rule ref="category/java/codestyle.xml/MethodNamingConventions">
    <properties>
      <property name="violationSuppressXPath">
        <value><![CDATA[
          .[
            ancestor::ClassDeclaration[1]
              /(ExtendsList | ImplementsList)
              /ClassType[
                @SimpleName = 'Library'
                and (
                  @PackageQualifier = 'com.sun.jna'
                  or ancestor::CompilationUnit
                    /ImportDeclaration[
                      @ImportedName = 'com.sun.jna.Library'
                      or @ImportedName = 'com.sun.jna'
                    ]
                )
              ]
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <!--
  UseDiamondOperator suggests replacing explicit constructor type
  arguments with a diamond '<>', but PMD only reports where it believes
  the diamond is a compilable replacement, and it reports four shapes
  where the replacement does not compile. Three are javac inference
  divergences seen in cactoos: a constructor whose arguments include an
  implicitly-typed lambda (javac loses the wildcard capture that flows
  through the lambda), a constructor whose arguments include a method
  reference (the diamond makes an overloaded constructor ambiguous), and
  a constructor that is the body of a lambda (its target type is an
  as-yet-uninferred type variable, so the diamond cannot be pinned); in
  all three the explicit type arguments are the only thing that makes
  inference succeed. The fourth is an anonymous class instantiation, such
  as 'new Supplier<String>() { ... }': '<>' on an anonymous class body is
  only legal at source level 9+, so the advice breaks the '-source 8'
  builds that qulice/jcabi-parent projects commonly target. The
  violationSuppressXPath below skips those four shapes and keeps the rule
  for the plain case, such as 'new ArrayList<String>()'. This is the PMD 7
  successor of #1242.
  Downstream: https://github.com/yegor256/qulice/issues/1690
  Downstream: https://github.com/yegor256/qulice/issues/1685
  Downstream: https://github.com/yegor256/qulice/issues/1242
  -->
  <rule ref="category/java/codestyle.xml/UseDiamondOperator">
    <properties>
      <property name="violationSuppressXPath">
        <value><![CDATA[
          .[
            ../../ArgumentList/LambdaExpression[@ExplicitlyTyped = false()]
            or ../../ArgumentList/MethodReference
            or ../../parent::LambdaExpression
            or ../../AnonymousClassDeclaration
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule ref="category/java/design.xml">
    <!--
    LoosePackageCoupling reports nothing until its 'packages' property
    names the packages to watch. Qulice ships one ruleset to every
    project and cannot know those names, so PMD drops the rule as
    misconfigured and prints a warning on every run.
    -->
    <exclude name="LoosePackageCoupling"/>
    <!--
    LawOfDemeter reports every chained call, including the fluent builders
    and Cactoos pipelines that Qulice's own style is built on. It fires 18
    times here and has no property to tell a fluent chain from a train
    wreck.
    -->
    <exclude name="LawOfDemeter"/>
    <!--
    SignatureDeclareThrowsException forbids 'throws Exception', which is
    the honest signature for a test method that exercises code throwing
    several checked types. It fires 13 times here, all in tests.
    -->
    <exclude name="SignatureDeclareThrowsException"/>
    <!--
    ExcessiveImports counts imports per file, which for a test class grows
    with the number of matchers and fixtures it names rather than with the
    coupling of the code under test.
    -->
    <exclude name="ExcessiveImports"/>
    <!--
    CyclomaticComplexity counts branches per method and per class. Qulice
    already bounds method length and nesting depth through Checkstyle,
    which constrains the same thing without a second threshold to tune.
    -->
    <exclude name="CyclomaticComplexity"/>
    <!--
    FinalFieldCouldBeStatic asks that a final field with a constant
    initializer become 'static final'. Qulice discourages static state,
    and the rule also pushes towards the inline initializers that
    'ConstructorShouldDoInitialization' below forbids.
    -->
    <exclude name="FinalFieldCouldBeStatic"/>
    <!--
    TooManyMethods is excluded because it counts every method in the
    class body and cannot be told to skip test classes, where one
    assertion per test method (as required by
    UnitTestContainsTooManyAsserts) inflates the count past any useful
    threshold. Disabling it for test classes through a
    violationSuppressXPath is not an option either: PMD consults the
    @SuppressWarnings suppressor before the XPath one, so a redundant
    @SuppressWarnings("PMD.TooManyMethods") in a test class is credited
    with the suppression and never reported by
    UnnecessaryWarningSuppression. We use our own TooManyMethodsRule
    instead, which skips test classes outright and counts only public
    and protected methods.
    Downstream: https://github.com/yegor256/qulice/issues/1605
    Downstream: https://github.com/yegor256/qulice/issues/1647
    Downstream: https://github.com/yegor256/qulice/issues/1656
    -->
    <exclude name="TooManyMethods"/>
    <!--
    ExceptionAsFlowControl is excluded because it flags legitimate
    validate-and-recover patterns where several guard clauses inside a
    single try block share one fallback path in the catch. The only
    "fix" the rule allows is to duplicate the catch body across N
    methods or wrap each throw in its own try, both of which are
    strictly worse than the original code.
    Downstream: https://github.com/yegor256/qulice/issues/1609
    -->
    <exclude name="ExceptionAsFlowControl"/>
  </rule>
  <rule ref="category/java/documentation.xml">
    <!--
    CommentRequired duplicates the Javadoc rules Qulice already runs
    through Checkstyle, which report a missing docblock with a better
    message and know about inherited documentation.
    -->
    <exclude name="CommentRequired"/>
    <!--
    CommentSize caps a comment at six lines and eighty columns. Qulice's
    own docblocks explain why a rule is off, which takes more than six
    lines, and Checkstyle already bounds the line length. It fires 100
    times here.
    -->
    <exclude name="CommentSize"/>
  </rule>
  <rule ref="category/java/errorprone.xml">
    <!--
    AvoidDuplicateLiterals counts a literal repeated four times anywhere
    in a file, which in tests means the same fixture name written once per
    test method. Extracting it into a constant is what Qulice's own test
    guidance forbids, since tests must not share state.
    -->
    <exclude name="AvoidDuplicateLiterals"/>
    <!--
    AvoidFieldNameMatchingMethodName forbids a field and a method sharing
    a name, which is the shape Qulice asks for: an immutable attribute
    named as a noun, exposed by a query method of the same noun, as in
    'name' and 'name()'.
    -->
    <exclude name="AvoidFieldNameMatchingMethodName"/>
    <!--
    AvoidLiteralsInIfCondition flags a literal in a condition even when
    the literal is the boundary being tested, such as comparing a count
    against zero or one. Naming those would add a constant that says less
    than the digit it replaces.
    -->
    <exclude name="AvoidLiteralsInIfCondition"/>
    <!--
    MissingSerialVersionUID asks every Serializable class for a
    'serialVersionUID' constant. That constant is a public static literal,
    which Qulice forbids, and 'NonSerializableClass' above already guards
    the part of serialization that breaks at runtime.
    -->
    <exclude name="MissingSerialVersionUID"/>
    <exclude name="TestClassWithoutTestCases"/>
    <!--
    CompareObjectsWithEquals is excluded from the bulk import here and
    re-enabled below with an extended 'typesThatCompareByReference' so
    that a 'java.util.concurrent.Future' is allowed to be compared with
    '=='. ErrorProne's UndefinedEquals / CollectionUndefinedEquality
    forbid calling equals() on a Future (it has no well-defined equality
    semantics), so reference identity is the only sound way to recognise
    the same future; without this override the two checkers contradict
    each other on the same line. This mirrors 0.30.4 disabling
    OperatorPrecedence for the analogous Checkstyle/ErrorProne
    contradiction (#1705).
    Downstream: https://github.com/yegor256/qulice/issues/1717
    -->
    <exclude name="CompareObjectsWithEquals"/>
  </rule>
  <!--
  CompareObjectsWithEquals wants equals() for every object comparison,
  but a 'java.util.concurrent.Future' has no meaningful equals() contract,
  so ErrorProne's UndefinedEquals fires on 'future.equals(other)' and its
  CollectionUndefinedEquality fires on a Future used as a Map key. Adding
  'java.util.concurrent.Future' to 'typesThatCompareByReference' (whose
  defaults are 'java.lang.Enum,java.lang.Class') lets PMD accept the '=='
  that is the only sound comparison, and since the rule matches by subtype
  it also covers 'java.util.concurrent.CompletableFuture' and the rest of
  the Future family. The broader interfaces in ErrorProne's
  TypesWithUndefinedEquality list (CharSequence, Collection, Iterable, ...)
  are deliberately left out: the rule matches subtypes, so listing them
  would also stop flagging '==' on a 'String', a 'List' or a 'Set', where
  reference comparison usually is a bug.
  Downstream: https://github.com/yegor256/qulice/issues/1717
  -->
  <rule ref="category/java/errorprone.xml/CompareObjectsWithEquals">
    <properties>
      <property name="typesThatCompareByReference" value="java.lang.Enum,java.lang.Class,java.util.concurrent.Future"/>
    </properties>
  </rule>
  <!--
  Recognise io.github.artsok annotations (@RepeatedIfExceptionsTest,
  @ParameterizedRepeatedIfExceptionsTest) as test cases so that
  TestClassWithoutTestCases does not flag classes that use them.
  Also recognise non-JUnit test conventions such as g4s8/oot, which
  uses a bare 'public static void test()' method as the entry point
  with no test annotation.
  Upstream: https://github.com/yegor256/qulice/issues/1521
  Downstream: https://github.com/yegor256/qulice/issues/1064
  -->
  <rule ref="category/java/errorprone.xml/TestClassWithoutTestCases">
    <properties>
      <property name="violationSuppressXPath">
        <value><![CDATA[
          .[
            .//Annotation[
              @SimpleName='RepeatedIfExceptionsTest'
              or @SimpleName='ParameterizedRepeatedIfExceptionsTest'
            ]
            or
            ClassBody/MethodDeclaration[
              @Name='test' and @Static=true() and @Visibility='public'
            ]
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule ref="category/java/performance.xml">
    <!--
    AvoidInstantiatingObjectsInLoops reports any 'new' inside a loop,
    which is how an immutable object is built per iteration. Hoisting the
    allocation out means mutating one instance, which Qulice forbids
    outright. It fires 15 times here.
    -->
    <exclude name="AvoidInstantiatingObjectsInLoops"/>
  </rule>
  <rule ref="category/java/multithreading.xml">
    <!--
    AvoidUsingVolatile reports the keyword itself, with no way to say that
    this particular field is read and written from two threads on purpose.
    A linter cannot tell a misuse of 'volatile' from the one case it is
    made for.
    -->
    <exclude name="AvoidUsingVolatile"/>
    <!--
    UseConcurrentHashMap asks that every Map become a ConcurrentHashMap,
    including the ones never shared between threads, which trades a
    cheaper map for a lock nobody contends. The choice belongs to the code
    that knows whether the map escapes.
    -->
    <exclude name="UseConcurrentHashMap"/>
    <!--
    DoNotUseThreads forbids 'new Thread', which is aimed at Java EE
    containers that manage their own pools. Qulice runs as a build plugin
    and starts threads on purpose, seven times here.
    -->
    <exclude name="DoNotUseThreads"/>
  </rule>
  <rule ref="category/java/security.xml"/>
  <rule name="UnitTestShouldIncludeAssert" message="This unit test should include assert() or fail()" language="java" class="com.qulice.pmd.rules.UnitTestShouldIncludeAssertRule">
    <description>
      Unit tests should include at least one assertion. This variant of
      PMD's UnitTestShouldIncludeAssert rule looks for assertions placed
      inside lambda bodies as well, which the upstream rule misses.
    </description>
    <priority>3</priority>
  </rule>
  <rule name="UnitTestContainsTooManyAsserts" message="Unit tests should not contain more than 1 assert(s)." language="java" class="com.qulice.pmd.rules.UnitTestContainsTooManyAssertsRule">
    <description>
      Unit tests should not contain more than one assertion. Unlike
      PMD's UnitTestContainsTooManyAsserts rule, this variant only
      counts calls whose simple name is one of the known JUnit /
      Hamcrest assertion methods (assertThat, assertEquals, assertTrue,
      fail, ...) instead of counting any identifier with an 'assert',
      'check' or 'verify' prefix. Calls such as Mockito.verify(...) or
      pull.checks() are therefore not flagged. Calls to assertThrows
      are also not counted, so a test that wraps
      assertThrows(...).getMessage() inside an assertThat to verify the
      thrown exception's message is not flagged.
    </description>
    <priority>3</priority>
  </rule>
  <rule name="TooManyMethods" message="This class has too many public and protected methods, consider refactoring it." language="java" class="com.qulice.pmd.rules.TooManyMethodsRule">
    <description>
      A class with too many methods is probably a good suspect for
      refactoring, in order to reduce its complexity and find a way to
      have more fine grained objects. Unlike PMD's TooManyMethods rule,
      this variant counts only public and protected methods, since
      private and package-private ones are implementation detail rather
      than part of the contract of the class. Test classes are not
      checked at all, because one assertion per test method inflates
      their method count beyond any useful threshold. Types that extend
      com.sun.jna.Library are skipped too, since they mirror a native
      library method by method.
    </description>
    <priority>3</priority>
  </rule>
  <rule name="ProhibitPlainJunitAssertionsRule" message="Avoid using Plain JUnit assertions" language="java" class="com.qulice.pmd.rules.ProhibitPlainJunitAssertionsRule">
    <description>
      Instead of using plain JUnit assertions like org.junit.Assert.assert*
      junit.framework.Assert.assert* - use Matchers from package org.hamcrest
    </description>
  </rule>
  <rule name="UseStringIsEmptyRule" message="Use String.isEmpty() when checking for empty string" language="java" class="com.qulice.pmd.rules.UseStringIsEmptyRule">
    <description>
      Method String.isEmpty() should be used when testing for empty string,
      rather than using
      String.length() compared to 0
    </description>
  </rule>
  <rule name="UseCollectionsSingletonList" message="Use Collections.singletonList instead of Arrays.asList with a single non-array argument" language="java" class="com.qulice.pmd.rules.UseCollectionsSingletonListRule">
    <description>
      Arrays.asList with a single non-array argument allocates a one-element
      backing array via varargs and wraps it in a list. Collections.singletonList
      expresses the same intent with a single, immutable, fixed-size list and
      avoids the implicit array allocation.
    </description>
    <priority>3</priority>
  </rule>
  <rule name="ProhibitFormatInLoggerRule" message="Avoid String.format inside jcabi-log Logger calls; pass the format string and arguments directly" language="java" class="com.qulice.pmd.rules.ProhibitFormatInLoggerRule">
    <description>
      jcabi-log Logger methods (trace, debug, info, warn, error) already
      accept a printf-style format string with positional arguments, so
      pre-formatting the message with String.format is redundant. Inline
      the format string and pass the arguments directly to the Logger
      method instead.
    </description>
    <priority>3</priority>
  </rule>
  <rule name="UnnecessaryLocalRule" message="Avoid creating unnecessary local variables like ''{0}''" language="java" class="com.qulice.pmd.rules.UnnecessaryLocalRule">
  </rule>
  <rule name="ImplicitFunctionalInterface" message="Annotate this interface with @FunctionalInterface or with @SuppressWarnings(&quot;PMD.ImplicitFunctionalInterface&quot;) to clarify your intent." language="java" class="com.qulice.pmd.rules.ImplicitFunctionalInterfaceRule">
    <description>
      An interface with a single abstract method should be annotated with
      @FunctionalInterface to clarify intent. Unlike PMD's built-in
      ImplicitFunctionalInterface rule, this variant does not fire when the
      interface extends a super-interface that PMD cannot resolve, since the
      total number of inherited abstract methods is then unknown and the
      suggested annotation might not compile.
    </description>
    <priority>3</priority>
  </rule>
  <rule name="ConstructorShouldDoInitialization" message="Avoid doing field initialization outside constructor." language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule">
    <description>
      Avoid doing field initialization outside class constructor.
      Such initialization is allowed only when there is no constructor.
    </description>
    <priority>3</priority>
    <properties>
      <property name="xpath">
        <value><![CDATA[
          //ClassDeclaration
          [count(ClassBody/ConstructorDeclaration)>0]
          [count(ClassBody/FieldDeclaration[@Static=false()]/VariableDeclarator[@Initializer=true()])>0]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule name="OnlyOneConstructorShouldDoInitialization" message="Avoid field initialization in several constructors." language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule">
    <description>
      Avoid doing field initialization in several constructors.
      Only one main constructor should do real work.
      Other constructors should delegate initialization to it.
    </description>
    <priority>3</priority>
    <properties>
      <property name="xpath">
        <value><![CDATA[
          //ClassDeclaration
              [count(ClassBody/ConstructorDeclaration)>1]
              [count(ClassBody/ConstructorDeclaration/Block[ExpressionStatement])>1]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule name="ConstructorOnlyInitializesOrCallOtherConstructors" message="Only field initialization or call to other constructors in a constructor." language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule">
    <description>
      Avoid putting anything other than field assignments into constructors.
      The only exception should be calling other constructors
      or calling super class constructor.
    </description>
    <priority>3</priority>
    <properties>
      <property name="xpath">
        <value><![CDATA[
          //ConstructorDeclaration/Block[
              count(ExpressionStatement[not(.//LambdaExpression)]) + count(LocalVariableDeclaration)
              !=
              count(ExpressionStatement/AssignmentExpression[not(.//LambdaExpression)]/FieldAccess/ThisExpression)
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <!--
  Direct access to a static final field inside a static initializer is
  allowed because qualifying it with the class name would not compile
  (a final field can only be assigned once by its simple name).
  Downstream: https://github.com/yegor256/qulice/issues/719
  -->
  <rule name="AvoidDirectAccessToStaticFields" message="Static fields should be accessed in a static way [CLASS_NAME.FIELD_NAME]." language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule">
    <description>
      Avoid accessing static fields directly.
    </description>
    <priority>3</priority>
    <properties>
      <property name="xpath">
        <value><![CDATA[
          //VariableAccess[
            @Name = //FieldDeclaration[@Static=true()]/VariableDeclarator/VariableId/@Name
            and not(ancestor::Initializer[@Static=true()])
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule name="AvoidAccessToStaticMembersViaThis" message="Static members should be accessed in a static way [CLASS_NAME.FIELD_NAME], not via instance reference." language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule">
    <description>
      Avoid accessing static fields or methods via instance with 'this' keyword.
    </description>
    <priority>3</priority>
    <properties>
      <property name="xpath">
        <value><![CDATA[
          //FieldAccess[
            ThisExpression
            and
            @Name =
              ancestor::ClassDeclaration[1]
                //FieldDeclaration[@Static=true()]
                  /VariableDeclarator
                  /VariableId/@Name
          ]
          |
           //MethodCall[
            ThisExpression
            and
            @MethodName =
              ancestor::ClassDeclaration[1]
                /ClassBody
                /MethodDeclaration[@Static=true()]
                  /@Name
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule name="ProhibitPublicStaticMethods" message="Public static methods are prohibited." language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule">
    <description>
      Public static methods are prohibited.
    </description>
    <priority>3</priority>
    <properties>
      <property name="xpath">
        <value><![CDATA[
          //MethodDeclaration[
              @Static=true() and
              @Visibility='public' and not
              (@Name='main' and FormalParameters/FormalParameter/ArrayType/ClassType/@SimpleName='String' and VoidType) and not
              (ModifierList/Annotation[@SimpleName='BeforeClass' or @SimpleName='AfterClass' or @SimpleName='Parameters'])
          ]
      ]]></value>
      </property>
    </properties>
  </rule>
  <rule name="ProhibitFilesCreateFileInTests" message="Files.createFile should not be used in tests, replace them with @Rule TemporaryFolder" language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule">
    <description>
      Files.createFile shouldn't be used in tests.
    </description>
    <priority>3</priority>
    <properties>
      <property name="xpath">
        <value><![CDATA[
          //ClassDeclaration[ends-with(@SimpleName, 'Test')]//MethodCall[@MethodName='createFile' and TypeExpression/ClassType/@SimpleName='Files']
        ]]></value>
      </property>
    </properties>
  </rule>
  <rule name="JUnitTestClassShouldBeFinal" language="java" class="net.sourceforge.pmd.lang.rule.xpath.XPathRule" message="JUnit test class should be final">
    <description><![CDATA[
      Reports on JUnit test classes declared non-final.
    ]]></description>
    <priority>3</priority>
    <properties>
      <!-- Solve priority conflict -->
      <property name="xpath">
        <value><![CDATA[
          //ClassDeclaration[
            @Interface = false()
            and
            @Final = false()
            and
            ancestor::CompilationUnit/ImportDeclaration[
              @ImportedName = 'junit.framework.TestCase'
              or
              @ImportedName = 'org.junit.Test'
              or
              @ImportedName = 'org.junit.jupiter.api.Test'
            ]
          ]
        ]]></value>
      </property>
    </properties>
  </rule>
</ruleset>
