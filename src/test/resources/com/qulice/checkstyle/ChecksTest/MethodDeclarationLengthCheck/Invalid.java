/*
 * This is not a real Java class. It won't be compiled ever. It is used
 * only as a text resource in integration.ChecksIT.
 */
public class Invalid {
    public Invalid(final int max)
        throws IOException {
    }

    public void foo(final int x)
        throws Exception {
    }
}
