# SPDX-FileCopyrightText: Copyright (c) 2011-2026 Yegor Bugayenko
# SPDX-License-Identifier: MIT
---
# yamllint disable rule:line-length
docker:
  image: yegor256/java
assets:
  settings.xml: yegor256/home#assets/qulice/settings.xml
  secring.gpg: yegor256/home#assets/secring.gpg
  pubring.gpg: yegor256/home#assets/pubring.gpg
install: |
  pdd --file=/dev/null
merge:
  script: |-
    mvn clean install -ntp -Pqulice-profile --errors --batch-mode --no-transfer-progress -Dstyle.color=never
    mvn verify -Pqulice -DskipTests -DskipITs --errors --batch-mode --no-transfer-progress -Dstyle.color=never
release:
  pre: false
  sensitive:
    - settings.xml
  script: |-
    [[ "${tag}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || exit -1
    mvn versions:set "-DnewVersion=${tag}"
    git commit -am "${tag}"
    mvn clean install -ntp -Pqulice-profile --errors --settings ../settings.xml --batch-mode --no-transfer-progress -Dstyle.color=never
    mvn deploy -Pqulice-profile -Pqulice -Psonatype -DskipTests -DskipITs --errors --settings ../settings.xml --batch-mode --no-transfer-progress -Dstyle.color=never
    mvn clean site-deploy -Pgh-pages -Pqulice-profile -Psite --errors --settings ../settings.xml --batch-mode --no-transfer-progress -Dstyle.color=never
