/*
 * Hello.
 */
package foo.bar;

/**
 * Simple.
 * @since 1.0
 */
public class ValidIndentation {
    /**
     * Ctor.
     */
    public ValidIndentation() {
        Arrays.asList(
            new Integer(
                1
            )
        )
    }
}
